# Gallery

个人媒体库：把一棵磁盘目录树呈现为可浏览、可消费的图库，并通过 Web 与原生客户端访问。

## Language

### 图库结构

**图库 (Library)**:
用户全部媒体的集合。由一个物理根目录界定，减去排除目录，加上虚拟路径。
_Avoid_: 媒体库、资源库、根目录

**文件夹 (Folder)**:
图库中的一个层级节点，可同时包含子文件夹与媒体。装了图的文件夹就是相册——"相册"不是独立概念。
_Avoid_: 相册、目录、Album、Directory

**媒体 (Media)**:
图库中可被观看的单件内容，只有图片与视频两类。txt/html/url 等文件存在于文件夹中但不是媒体。
_Avoid_: 资源、条目、Item

**排除目录 (Exclude)**:
被声明为不属于图库的物理目录。它们完全不可见，而非"隐藏"。
_Avoid_: 黑名单、屏蔽目录

**虚拟路径 (Virtual Path)**:
把散落在不同物理位置的若干文件夹聚合成的一个逻辑文件夹。对观看者而言与普通文件夹无异。
_Avoid_: 聚合目录、软链、合集

### 观看方式

**本层视图 (Shallow View)**:
某个文件夹直接子项的呈现：子文件夹与本层媒体混排。回答"这一层有什么"。
_Avoid_: explore 模式、album 模式、浏览模式

**递归视图 (Recursive View)**:
某个文件夹所有子孙媒体拉平后的呈现，不含文件夹。回答"这底下所有的东西"。
_Avoid_: image 模式、照片模式、瀑布流

### 消费方式

观看一件媒体的方式由三个**互相正交**的维度决定：顺序、推进、窗口形态。三者任意组合都
成立，不存在"套餐"。

**随机顺序 (Shuffle)**:
播放序列被随机排列。是播放器的一个开关，既不是一种视图，也不是一个动作。
_Avoid_: random 模式、随机模式、随机播放

**自动前进 (Auto-advance)**:
播放器按固定间隔自动切到下一件媒体。与随机顺序正交——原序也可以自动前进。
_Avoid_: 幻灯片、slideshow、轮播

**悬浮模式 (Floating Mode)**:
仅桌面端的一种窗口形态：无边框、常驻其他窗口之上的小窗。与顺序、推进两个维度正交。
_Avoid_: 置顶窗口、小窗模式、画中画

### 派生物

**缩略图 (Thumbnail)**:
图片的缩小版本，用于成片呈现。
_Avoid_: 预览图、小图

**封面 (Poster)**:
视频的代表帧，使视频能与图片一样参与成片呈现。存在"尚未就绪"这一合法状态——那不是错误。
_Avoid_: 首帧、预览帧、cover

### 标注

**标签 (Tag)**:
对单张图片内容的机器标注，每条带一个置信度。描述"图里有什么"。
_Avoid_: 关键词、分类

**说明 (Caption)**:
对单张图片的一句自然语言描述。与标签是不同粒度的标注，不互相替代。
_Avoid_: 描述、注释、alt

---

## 废弃说法

以下出现在现有 Web 前端与文档中，**不再是领域概念**，新客户端不应引入：

| 废弃说法 | 实际含义 |
|---|---|
| `album` 模式 | 本层视图，隐藏媒体、只列文件夹 |
| `explore` 模式 | 本层视图 |
| `image` 模式 | 递归视图 |
| `random` 模式 | 随机顺序 + 自动前进 + 全屏，三者被打包成了一个"模式" |
| "模式切换" | 递归开关 |
| 灯箱 Slideshow 插件 | 自动前进（与随机顺序正交，不该是插件） |

三个"模式"只是两个正交维度（是否递归、是否列出文件夹）的部分组合。Web 前端自身
已在代码中承认这份冗余：落在叶子文件夹时会强制从 `album`/`explore` 切走。
