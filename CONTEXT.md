# Gallery

个人媒体库：把一棵磁盘目录树呈现为可浏览、可消费的图库，并通过 Web 与原生客户端访问。

## Language

### 图库结构

**图库 (Library)**:
用户全部媒体的集合。由一个物理根目录界定，减去排除目录，加上虚拟路径。
_Avoid_: 媒体库、资源库、根目录

**文件夹 (Folder)**:
图库中的一个层级节点，可同时包含子文件夹与媒体。装了图的文件夹就是相册——"相册"不是独立概念。
_Avoid_: 相册、目录、Album、Directory

**媒体 (Media)**:
图库中可被观看的单件内容，只有图片与视频两类。txt/html/url 等文件存在于文件夹中但不是媒体。
_Avoid_: 资源、条目、Item

**排除目录 (Exclude)**:
被声明为不属于图库的物理目录。它们完全不可见，而非"隐藏"。
_Avoid_: 黑名单、屏蔽目录

**虚拟路径 (Virtual Path)**:
把散落在不同物理位置的若干文件夹聚合成的一个逻辑文件夹。对观看者而言与普通文件夹无异。

有一个后果值得单独记住：**它底下的媒体路径不以它自己的路径为前缀**。虚拟文件夹
`奶酪坏了` 里的图片路径是 `Twitter/cheesegonebad77/…`。任何"子项路径 = 父路径 + /"
的假设都是错的。
_Avoid_: 聚合目录、软链、合集

### 观看方式

一个文件夹有三种被看的方式，由两个维度决定：**是否递归** × **列文件夹还是列媒体**。
三者的名字沿用 Web 既有的那一套，Web 与原生客户端共用同一套词汇，不另起中文名。

**explore**:
某个文件夹直接子项的呈现：子文件夹与本层媒体混排。回答"这一层有什么"。
_Avoid_: 本层视图、浏览模式

**album**:
该文件夹底下所有直接装了媒体的后代文件夹，拉平成一个列表，不含媒体本身。
回答"这底下有哪些相册"。是递归的——它不是 explore 的"只看文件夹"版本。
_Avoid_: 相册列表、递归目录

**image**:
该文件夹所有子孙媒体拉平后的呈现，不含文件夹。回答"这底下所有的东西"。
_Avoid_: 递归视图、照片模式、瀑布流

### 消费方式

播放器只负责播放**别人交给它的序列**——序列从哪来（当前视图，还是 `random`）是它不关心
的事。它自己只有一个可调维度：自动前进。不存在"套餐"。

**`random`**:
一个动作，不是视图：从当前文件夹底下随机取样，直接进播放器；退出播放器回到按下它之前
的视图。它与 explore/album/image 并排摆在同一个切换器里，但按下去不停留为选中态。

它给出的是一条**无限流**，不是一个排列——没有总数，没有"看完一轮"，同一件媒体可能
再次出现。取样由后端做（`/api/random/`），客户端成批要、边划边要。

_Avoid_: 随机模式、随机播放、洗牌

**自动前进 (Auto-advance)**:
播放器按固定间隔自动切到下一件媒体。与序列从哪来无关——视图里点进去的和 `random` 给的
序列，都可以自动前进。
_Avoid_: 幻灯片、slideshow、轮播

### 派生物

**缩略图 (Thumbnail)**:
图片的缩小版本，用于成片呈现。
_Avoid_: 预览图、小图

**封面 (Poster)**:
视频的代表帧，使视频能与图片一样参与成片呈现。存在"尚未就绪"这一合法状态——那不是错误。
_Avoid_: 首帧、预览帧、cover

### 标注

**标签 (Tag)**:
对单张图片内容的机器标注，每条带一个置信度。描述"图里有什么"。
_Avoid_: 关键词、分类

**说明 (Caption)**:
对单张图片的一句自然语言描述。与标签是不同粒度的标注，不互相替代。
_Avoid_: 描述、注释、alt

---

## 废弃说法

| 废弃说法 | 实际含义 |
|---|---|
| `random` 是第四种视图 | 它是动作：取样 + 进播放器，退出即回原视图 |
| 灯箱 Slideshow 插件 | 自动前进（与序列从哪来无关，不该是插件） |
| "混合/隔离"是全局设置 | 只决定 `random` 取不取视频 |
| 随机是"打乱一个序列" | 是无限取样流，可重复、无总数 |
| 悬浮模式 / 画中画 | 已废除，不再是这个产品的一部分 |
